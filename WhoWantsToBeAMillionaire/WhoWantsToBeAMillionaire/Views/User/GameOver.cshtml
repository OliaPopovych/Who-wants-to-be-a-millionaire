@model IEnumerable<WhoWantsToBeAMillionaire.Models.User>

@{
    ViewBag.Title = "GameOver";
}

<div class="title-game-over">
    <h2>You lose or You Win</h2>
</div>

<div class="winning">
    <h1>You won 1000000 $</h1>
</div>

<input type="button" value="Play Again" onclick="location.href='http://localhost:56835/User/Start'" class="btn btn-default btn-new-game" />

<div class="statistic">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Winning)
            </th>
            <th></th>
        </tr>


        @*@foreach (var item in Model) {*@
        <tr>
            <td>
                @*@Html.DisplayFor(modelItem => item.Name)*@
                Name
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.Email)*@
                Email
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.EarnedMoney)*@
                Winning
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
            </td>
        </tr>

    </table>
</div>

